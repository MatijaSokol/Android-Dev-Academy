<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="constraint" parent="TextAppearance.AppCompat">
        <item name="android:background">@color/purple</item>
        <item name="android:paddingLeft">@dimen/constraint_padding</item>
        <item name="android:paddingRight">@dimen/constraint_padding</item>
    </style>

    <style name="linear" parent="TextAppearance.AppCompat">
        <item name="android:gravity">center</item>
        <item name="android:layout_marginTop">@dimen/margin_top</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="textView" parent="TextAppearance.AppCompat">
        <item name="android:layout_width">@dimen/match_linear</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/textView_text_size</item>
    </style>

    <style name="editText" parent="TextAppearance.AppCompat">
        <item name="android:layout_width">@dimen/match_linear</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:inputType">numberDecimal</item>
        <item name="android:textSize">@dimen/textView_text_size</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="button" parent="TextAppearance.AppCompat">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/calculate</item>
        <item name="android:layout_marginTop">@dimen/margin_top</item>
    </style>

</resources>
